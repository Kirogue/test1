# Acolyte Fight

This is a simple prototype for an improved version of **Acolyte Fight.io** built with Node.js, Express, and Socket.IO.

## Setup

```bash
npm install
node server/app.js
```

Open [http://localhost:3000](http://localhost:3000) in your browser to play.
